---
import CopyMap from './CopyMap.astro';

interface Props {
  summary: string;
  detail: Array<string>;
}

const { summary, detail } = Astro.props;
---

<details class="accordion">
  <summary>{ summary }</summary>

  <CopyMap copy={ detail } />
</details>